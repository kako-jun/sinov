# MYPACE API endpoint (for publishing posts)
# ローカル開発: http://localhost:8787
# 本番: https://your-mypace-api.workers.dev
API_ENDPOINT=http://localhost:8787

# Ollama settings (optional, for LLM-powered content generation)
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3.2:3b

# Note: Bot keys (nsec) are managed in bots/keys.json, not in .env
# This keeps the secrets secure and allows for 100+ bots without cluttering .env
