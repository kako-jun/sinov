# 構想ドキュメント

このフォルダには**未実装**の構想・アイデアを残しています。
実装済みの仕様は `docs/` を参照してください。

## 未実装の構想

### 追加パラメータ

`parameters.md` に記載の以下のパラメータは未実装:

| パラメータ | 説明 |
|-----------|------|
| `event_enthusiasm` | イベント熱狂度（0.0〜1.0） |
| `contrarian` | 逆張り度 |
| `eccentricity` | 不思議ちゃん度 |

### 記者システム

`reporters.md` に記載の専門記者NPC:

- `reporter_tech`: IT・テクノロジー担当
- `reporter_game`: ゲーム担当
- `reporter_creative`: 創作・アート担当
- `reporter_general`: 一般時事担当
- `reporter_trend`: Xトレンド担当（厳格フィルタ）

現状はRSSクライアントで取得しているが、専門記者NPCとしての実装はなし。

### Xトレンド連携

`reporters.md` に記載のXトレンドフィルタリング:

- ホワイトリスト方式（季節イベント、ゲームリリース等のみ許可）
- ブラックリスト（政治、事件、炎上等を除外）

### 分散アーキテクチャ

`architecture.md` に記載の「天から見下ろさない」設計:

- 各NPCが自分のファイルだけを見て判断
- 中央コントローラーなし
- ファイルが「場」として機能

現状はtickコマンドで一括処理するアーキテクチャ。

## アイデアメモ

- `character-ideas.md`: キャラクター名・職業・設定のアイデア

## 実装済み（参考）

以下は実装済みのため `docs/` に移動:

- 記憶システム（短期・長期・連作）
- 性格パラメータ（11種類）
- 関係性（グループ・ペア・ストーカー）
- 文章スタイル加工
- 投稿キュー
- レビューシステム
- Nostr連携
